export const environment = {
  production: true,
  baseUrl: window.location.origin, // Base URL here
  apiUrl: 'https://your_site/api' // global API URL (for production build)
  //... more of your variables
};
